version: 0.2

env:
  git-credential-helper: yes

phases:
  install:
    commands:
      - yum install -y yum-utils
      - yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
      - yum install -y terraform

  pre_build:
    commands:
      - echo "Requested tag ${GIT_TAG}"
      - git remote -v
      - git status
      - echo "Pipeline commit ${CODEBUILD_RESOLVED_SOURCE_VERSION}"
      - |
        if [ -n "${GIT_TAG}" ] && git ls-remote --exit-code --tags origin "refs/tags/${GIT_TAG}" >/dev/null 2>&1; then
          echo "Fetching tag ${GIT_TAG}"
          git fetch origin "refs/tags/${GIT_TAG}:refs/tags/${GIT_TAG}"
          git checkout "tags/${GIT_TAG}"
        else
          echo "Tag '${GIT_TAG}' not found; checking out branch ${GIT_BRANCH}"
          git fetch origin "refs/heads/${GIT_BRANCH}:refs/remotes/origin/${GIT_BRANCH}"
          git checkout -B "${GIT_BRANCH}" "origin/${GIT_BRANCH}"
        fi

      - git rev-parse HEAD

  build:
    commands:
      - terraform init -input=false
      - terraform validate
      - terraform plan -input=false -out=tfplan

artifacts:
  files:
    - "**/*"